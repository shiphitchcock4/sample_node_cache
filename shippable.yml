# Language setting
language: node_js

# Version number
node_js:
  - 0.12

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  
  ci:
    - ps -eax
    - ls -al

  #post_ci:
   # - docker push shippabledocker/shiphitchcock1_sample_node
  cache: true

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
      
